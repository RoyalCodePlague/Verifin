{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && python -m pip install -r requirements.txt && python manage.py collectstatic --noinput",
    "watchPatterns": ["backend/**"]
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "cd backend && python manage.py migrate && python manage.py runserver 0.0.0.0:$PORT",
    "healthcheckPath": "/api/"
  }
}